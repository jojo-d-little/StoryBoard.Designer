StarterProjects

This folder contains built-in starter project root files discovered by the Create New dialog.

Rules:
- Any valid .sbe.json root project file can be used as a starter.
- Discovery scans this folder recursively.
- External starter selection is supported via Browse in the dialog.
