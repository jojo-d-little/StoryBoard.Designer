<UserControl x:Class="StoryboardDesigner.App.Views.Controls.RoomVerticalLookSlotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120"
             d:DesignWidth="320">
    <Border Margin="4" Padding="6" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock FontWeight="SemiBold" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </StackPanel>
            <Button Grid.Column="1"
                    HorizontalAlignment="Left"
                    Padding="10,4"
                    Command="{Binding DataContext.EditRoomImageSlotCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}">
                <StackPanel Orientation="Horizontal">
                    <Border Width="88" Height="50" BorderBrush="#D1D5DB" BorderThickness="1" Background="#F9FAFB" CornerRadius="4">
                        <Grid>
                            <Image Source="{Binding ThumbnailSource}" Stretch="UniformToFill" />
                            <TextBlock Text="No Image" Foreground="#6B7280" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="Edit" />
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</UserControl>
