<UserControl x:Class="StoryboardDesigner.App.Views.Controls.SettingsAccessPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="480"
             d:DesignWidth="320"
             AutomationProperties.AutomationId="MainWindow.SettingsAccessPane">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" FontSize="18" FontWeight="SemiBold" Foreground="#111827" Margin="0,0,0,10">
            Settings Access
        </TextBlock>
        <Border Background="#F9FAFB" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6" Padding="12">
            <StackPanel>
                <TextBlock Foreground="#374151" FontWeight="SemiBold" Text="Use the tree to edit settings" />
                <TextBlock Margin="0,8,0,0" Foreground="#6B7280" TextWrapping="Wrap"
                           Text="Select a Settings node directly in the Project Hierarchy to open its editor dialog." />
                <TextBlock Margin="0,10,0,0" Foreground="#6B7280" Text="Available nodes:" />
                <TextBlock Margin="8,4,0,0" Foreground="#4B5563" Text="Global Settings" />
                <TextBlock Margin="8,2,0,0" Foreground="#4B5563" Text="Planet Settings" />
                <TextBlock Margin="8,2,0,0" Foreground="#4B5563" Text="Country Settings" />
                <TextBlock Margin="8,2,0,0" Foreground="#4B5563" Text="Area Settings" />
                <TextBlock Margin="8,2,0,0" Foreground="#4B5563" Text="Room Settings" />
                <TextBlock Margin="8,2,0,0" Foreground="#4B5563" Text="Game Object Settings" />
            </StackPanel>
        </Border>
    </DockPanel>
</UserControl>
