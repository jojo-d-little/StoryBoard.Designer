<UserControl x:Class="StoryboardDesigner.App.Views.Controls.RoomImageSlotCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="180"
             d:DesignWidth="180">
    <Border Margin="4" Padding="6" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4">
        <StackPanel>
            <TextBlock FontWeight="SemiBold" FontSize="11" Text="{Binding SlotLabel, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Button Margin="0,6,0,0"
                    Padding="8,2"
                    HorizontalAlignment="Left"
                    Content="Edit Image"
                    Command="{Binding DataContext.EditRoomImageSlotCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}" />
            <Border Margin="0,6,0,0" Width="96" Height="56" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="4" Visibility="Collapsed">
                <Grid ClipToBounds="True" Background="#F9FAFB">
                    <Image Source="{Binding ThumbnailSource}" Stretch="UniformToFill" />
                </Grid>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
