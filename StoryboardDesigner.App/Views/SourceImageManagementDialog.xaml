<Window x:Class="StoryboardDesigner.App.Views.SourceImageManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Source Image Management"
        Width="920"
        Height="640"
        MinWidth="760"
        MinHeight="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Mode" />
        <ComboBox x:Name="ModeComboBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" SelectedIndex="0">
            <ComboBoxItem Tag="RepairMissingSources" Content="Repair Missing Sources" />
            <ComboBoxItem Tag="ConsolidateSourceImages" Content="Consolidate Source Images" />
        </ComboBox>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Options" />
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
            <CheckBox x:Name="RepointPathsCheckBox" IsChecked="True" Content="Repoint source paths when an unambiguous local match or consolidation target exists" />
        </StackPanel>

        <TextBox x:Name="PreviewTextBox"
                 Grid.Row="2"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 IsReadOnly="True"
                 FontFamily="Consolas"
                 FontSize="12" />

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="110" Margin="0,0,8,0" Content="Preview" Click="Preview_OnClick" />
            <Button x:Name="ApplyButton" Width="110" Margin="0,0,8,0" Content="Apply" Click="Apply_OnClick" IsEnabled="False" />
            <Button Width="110" Content="Close" Click="Close_OnClick" />
        </StackPanel>
    </Grid>
</Window>
