<Window x:Class="StoryboardDesigner.App.Views.ScopedProcedureOwnershipDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Procedures"
        Width="760"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText"
                   Grid.Row="0"
                   Margin="0,0,0,10"
                   FontWeight="SemiBold"
                   Text="Procedures" />

        <DataGrid x:Name="ProceduresGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Owned" Width="80" Binding="{Binding IsOwned, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Name" Width="2*" Binding="{Binding Name}" IsReadOnly="True" />
                <DataGridTextColumn Header="Summary" Width="2*" Binding="{Binding Summary}" IsReadOnly="True" />
                <DataGridTextColumn Header="Id" Width="2*" Binding="{Binding IdText}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="96" Margin="0,0,8,0" Content="Save" Click="Save_OnClick" />
            <Button Width="96" Content="Cancel" Click="Cancel_OnClick" />
        </StackPanel>
    </Grid>
</Window>
