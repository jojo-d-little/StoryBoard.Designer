using Storyboard.Shared.RuntimeContracts;
using Storyboard.Shared.GameStateData;

/*
namespace StoryboardDesigner.App.Models;

public static class ScopedAwareNodeRuntimeAdapter
{
    public static IRuntimeScopeNode? AsRuntimeScopeNode(this IScopedAwareNode? node)
    {
        return node is null ? null : new Adapter(node);
    }
}
*/
